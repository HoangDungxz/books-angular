import { AppInputBorderDirective } from "./app-input-border.directive";

describe('AppInputBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new AppInputBorderDirective();
    expect(directive).toBeTruthy();
  });
});
